server:
  port: 8071
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AntonioDiaz/playing_spring_microservices.git
          clone-on-start: true
          default-label: master
          search-paths: config